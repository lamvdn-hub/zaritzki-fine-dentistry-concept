**Badge** — a small uppercase status label (open/closed, category, "New patients welcome").

```jsx
<Badge variant="success" dot>Accepting patients</Badge>
<Badge variant="gold">Private &amp; statutory</Badge>
```

Variants: `neutral` / `gold` / `solid` / `success` / `warning` / `danger` / `info`. Sizes `sm` / `md`.
