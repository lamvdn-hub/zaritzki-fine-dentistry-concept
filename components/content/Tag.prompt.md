**Tag** — a rounded chip for filters, categories, and removable selections. Pill-shaped (the one place we go fully round).

```jsx
<Tag onClick={...} selected>Implantology</Tag>
<Tag onRemove={...}>Whitening</Tag>
```

`selected` gives the filled espresso state; `onRemove` adds a × affordance.
