**Radio** — single-choice control within a group (share a `name`); a brass dot scales in on selection.

```jsx
<Radio name="time" label="Morning" defaultChecked />
<Radio name="time" label="Afternoon" />
```
