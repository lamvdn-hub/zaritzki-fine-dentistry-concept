**Switch** — a binary on/off toggle; track fills brass and the thumb slides when on. Use for settings and preferences, not for form submission choices (use Checkbox/Radio there).

```jsx
<Switch label="Send appointment reminders by SMS" defaultChecked />
```
