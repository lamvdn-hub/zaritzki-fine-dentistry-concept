**Tooltip** — a small espresso bubble on hover/focus, for icon-button labels and brief hints.

```jsx
<Tooltip label="Call the practice"><IconButton label="Call"><PhoneIcon/></IconButton></Tooltip>
```

`placement`: `top` (default) or `bottom`. Keep the label to a few words.
