# SDD ledger — plan: IMPLEMENTATION_PLAN.md

- Baseline commit: c33a539
- Working branch: feat/zaritzki-landing-page
- User review gate: stop after every phase and wait for confirmation.
- Approved correction: pin Next.js 15 and use npm consistently.
- Approved correction: behavioral tests use genuine RED/GREEN ordering.
- Approved correction: insurer reimbursement and signature-gated treatment claims remain unasserted; only confirmed neutral estimate wording may render.
- Verified correction: locale layout is the root layout; `/` redirects via Next.js 15 middleware.
- Verified correction: no price column; stale contrary handoff sentence removed.
- Task 1: fix round 1/5 (2 addressed, 0 open — E2E command and dev-server evidence; commits 3bf9882..da2e3e8)
- Task 1: complete (commits c33a539..da2e3e8, review clean)
- Task 2: minor (deferred): font E2E checks registered families but not each FontFace status.
- Task 2: minor (deferred): RED chronology is preserved in the report rather than reconstructable from separate commits.
- Task 2: complete (commits e12ff94..c4329a1, review clean)
- Task 3: fix round 1/5 (direct Playwright-managed Next process still hung after assertions; no commit)
- Task 3: fix round 2/5 (Windows Playwright lifecycle moved in-process; focused/full E2E exited cleanly; commit 337b8ee)
- Task 3: complete (commits 8295b98..337b8ee, rereview clean)
- Phase 1: verification complete (unit 6/6; E2E 6/6 with natural teardown; lint clean; production build clean; direction seed preserved; all five token files byte-identical)
- Phase 1: paused at the required user review gate before Phase 2.
- Phase 1: user approved; Phase 2 authorized (2026-08-11).
- Task 4: complete (commits 8cbef9a..81617c3, review clean)
- Task 5: minor (deferred): `useLocation` outside its provider throws clearly, but that guard lacks an observable regression test.
- Task 5: fix round 1/5 (2 addressed, 0 open — persisted LocationId own-property validation and prototype-key regression coverage; commits 37d891d..e0bf8ce)
- Task 5: complete (commits ffcb834..e0bf8ce, review clean with 1 deferred minor)
- Phase 2: verification complete (unit 25/25; pending CLI exactly 4; lint clean; production build clean; E2E 6/6 with natural teardown; direction seed preserved)
- Phase 2: paused at the required user review gate before Phase 3.
- Phase 2: user approved; Phase 3 authorized (2026-08-11).
- Task 6: complete (commits 7274aeb..32c75af, review clean)
- Task 7: complete (commits 3ef8a88..45fe34f, review clean)
- Task 8: fix round 1/5 (1 addressed, 0 open — narrow action-row overflow; commits 356fc4b..e5c112f)
- Task 8: complete (commits ea54628..e5c112f, review clean)
- Task 9: complete (commits f021416..2eddc1a, review clean)
- Phase 3: verification complete (unit 53/53; pending CLI exactly 4; lint clean; TypeScript clean; production build clean; E2E 6/6 with natural teardown; direction seed preserved)
- Phase 3: paused at the required user review gate before Phase 4.
- Phase 3: user approved; Phase 4 authorized (2026-08-11).
- Phase 4 correction: execute Tasks 11, 10, 12; keep the single rail static without JavaScript, fix it only during the desktop walk, and release it at `#practices`; keep Reveal content visible until client enhancement is available.
- Task 11: minor (deferred): the IntersectionObserver scroll-spy callback is not directly behavior-tested; initial `aria-current` and all dock/fix/release transitions are covered.
- Task 11: complete (commits 0b8abb0..c7d07c4, review clean with 1 deferred minor)
- Task 10: complete (commits 5f3c53f..de152e3, review clean)
- Task 12: complete (commits bd9ccd3..09dbda8, review clean)
- Phase 4: verification complete (unit 74/74; pending CLI exactly 4; lint clean; TypeScript clean; production build clean; E2E 6/6 with natural teardown; direction seed preserved in 5 built artifacts; port 3000 clear; worktree clean before phase-status update)
- Phase 4: paused at the required user review gate before Phase 5.
- Phase 4: user approved; Phase 5 authorized (2026-08-11).
- Phase 5 correction: move all four open facts into a typed registry; render self-pay wording only when known; give ClosingCta its own RED cycle; pass serializable messages into a client-owned page composition and validate query seeds with an own-property check.
- Task 13: complete (commits e80ac53..90cf297, review clean)
- Task 14: complete (commits 3af3b5c..1245607, review clean; focused 5/5, full unit 85/85, typed pending registry exactly 4, TypeScript clean)
- Task 15: minor (deferred): strengthen ProofBand regression coverage for the displayed 5.0 values and non-blockquote testimonial-like text.
- Task 15: minor (deferred): lock exact map destinations and telephone links in PracticesSection tests; production output is correct.
- Task 15: complete (commits e678e39..4049a04, review approved; three RED/GREEN cycles, focused 3/3 + 3/3 + 2/2, full unit 93/93, TypeScript clean)
- Task 16: minor (deferred): page order E2E locks direct `section[id]` nodes but not the relative positions of ProofBand and ClosingCta, which currently have no IDs.
- Task 16: complete (commits dce38df..4e9359c, review approved; focused E2E 3/3, full unit 93/93, full desktop/mobile E2E 12/12, lint/TypeScript/build clean)
- Task 16 visual-fix: anchored desktop walk content and mobile direct-hash content were measured beneath sticky/fixed chrome; added responsive document scroll clearance with real-link/direct-hash RED/GREEN coverage (commit 275e906, re-review approved).
- Task 16 visual-fix minor (deferred): anchor-clearance E2E asserts the lower bound but does not also bound the target within the viewport; captured RED proves current native scrolling is exercised.
- Phase 5: verification complete after the anchor fix (unit 93/93; pending CLI exactly 4; lint clean; TypeScript clean; production build clean; desktop/mobile E2E 16/16; direction seed preserved in built server artifact; responsive scroll clearance present in emitted CSS; port 3000 clear; worktree clean).
- Phase 5: visual inspection complete on the live dev server at desktop and mobile widths. Composed typography, content rhythm, cost/proof/practices/closing sections, location seeding, and anchor visibility were inspected; the only expected visual gaps are missing Task 17 photography assets. In-app browser connection was unavailable, so the project Playwright browser captured the live server views.
- Phase 5: paused at the required user review gate before Phase 6.
- Continuation handoff: user reported an unresolved smaller-display hero bug without an exact viewport or symptom. It is a binding Phase 6 Task 20 systematic-debugging/TDD/review acceptance gate; do not assume Task 17 imagery fixes it.
- Continuation handoff: Phase 5 user approval remains pending. User reported 14% weekly Codex allowance remaining and was advised to wait for reset before Phase 6; obtain fresh approval and budget confirmation before Task 17.
- Continuation handoff: final baseline is unit 93/93, desktop/mobile E2E 16/16, pending exactly 4, lint/TypeScript/build clean, direction seed preserved, responsive anchor clearance emitted, port 3000 clear, and worktree clean at `4e425dd` before the handoff documentation commit.
- Phase 5: user explicitly approved and renewed execution budget confirmed; Phase 6 authorized (2026-08-11). Resumed at `2cec43b` on `feat/zaritzki-landing-page`, worktree clean, port 3000 clear.
- Phase 6 pre-flight scan: no plan/constraint contradictions requiring a user ruling. One benign overlap noted — Task 19 `conversion.spec.ts` re-covers the `?praxis=` seeding already asserted in `page.spec.ts`; the implementer keeps the plan's test and does not delete the existing one.
- Phase 6 pre-flight verification: every literal string Task 19 asserts exists in `site/messages/en.json` (hero h1, statutory-patient line, insurance qualifier, CTA label, skip link, closing heading); `030 854 030 00` correctly lives in `locations.ts` as `PHONE_DISPLAY`. No banned voice phrase is present in the message catalogue.
- Task 17 sourcing protocol proven at controller level before dispatch: Unsplash photo pages return 401 to curl, so attribution may NOT be scraped with curl. Verified working path is WebSearch (allowed_domains unsplash.com) to find candidates, WebFetch on the photo page for photographer/profile/licence, and `https://unsplash.com/photos/<ID>/download?force=true&w=2400` which returns 200 and redirects to the real `images.unsplash.com` asset with the photographer slug in its `dl=` parameter as independent confirmation. Pexels returns 403 to curl and is not a usable source here.
- Standing Phase 6 constraint: sourcing Task 17 stock images does NOT resolve the `photography` open fact. It stays pending until the practice approves or replaces all twelve.
- Task 17: implementer returned DONE_WITH_CONCERNS at commit 510ff0b (twelve images, CREDITS.md, evidence file; unit 93/93, build clean, WebP delivery confirmed, locations.ts untouched).
- Task 17 review (independent, opus): spec ❌ / quality not approved. Attribution integrity verified clean — reviewer independently re-confirmed 6 of 12 photographers via WebFetch plus `dl=` slug cross-check; no fabricated credit found. Controller's aspect-ratio concern adjudicated by the reviewer as NOT a defect: every consumer uses `next/image` fill + object-fit cover in a CSS-fixed box, so switching practice causes no layout shift or page-geometry change.
- Task 17 minor (deferred): `mitte/treatment-room.jpg` contains two mounted taxidermy heads in near-black upper-left, outside the crop at every shipped viewport but undisclosed in CREDITS.md's "what is in each frame" audit.
- Task 17 minor (deferred): `task-17-report.md` mis-describes two slots (detail as "brass inlay on dark wood"; entrance as "at night" when the exterior is plainly daylit).
- Task 17: fix round 1/5 (6 addressed, 0 open — F1 devotional print in charlottenburg/lounge, F2 Moorish calligraphy door as mitte/detail, F3 hero white-blowout crop, F4 Charlottenburg register drift incl. a legible US NEMA outlet, F5 undisclosed taxidermy, F6 stale report descriptions; five images swapped; commits 510ff0b..95b9a25). Re-review independently re-verified all five new photographers and PSNR-matched each shipped file to its credited source.
- Task 17: controller-raised finding after round 1 — the round-1 hero replacement fixed the blowout but shipped a worn interior stairwell (stained plaster, peeling patches, pipe handrail). Failed step 01 "The street" as a threshold and anti-goal 3 (austere/intimidating over calming). Not a reviewer finding; raised by the controller against the comp caption and the Global Constraints.
- Task 17: fix round 2/5 (1 addressed, 0 open — hero re-sourced to Daniel Romero `VitXrx5ajeQ`, verified at both the 1.82:1 desktop and ~0.50:1 mobile centre crops; left third computes ~14.7:1 contrast against ivory, weakest 0.6-alpha qualifier ~6.0:1, both above AA; commit 95b9a25..88b5be4).
- Task 17 minor (deferred): the shipped hero is an interior door with **electronic keycard locksets** (Onity/VingCard/Salto pattern) and convention-hotel axminster carpet. The 1.82:1 desktop crop removes the carpet, marble apron and floor cable and shrinks the locks to small brass rectangles, reading as gracious arrival; the ~0.50:1 mobile crop retains all three tells and reads as a hotel guest-room door. Reviewer rated it material but non-blocking and better than both predecessors. Flag to the Task 20 finish review and to the user's Phase 6 visual review.
- Task 17 minor (deferred): the hero's warm mass peaks at 30-40% and 60-70% across, while the comp's `.ph` glow sits at 72%; the right edge is the darkest band. Opposite failure to a blowout and harmless, but relevant if the hero ever reads flat.
- Task 17 minor (deferred): a slack black floor cable sits below the desktop crop entirely, but is a discernible (not identifiable) dark squiggle at bottom-left on mobile under 0.90-0.82 scrim. CREDITS.md's "no word, letter or mark" claim about the transom strip is also stronger than the pixels support; "nothing legible at any shipped size" is the accurate form.
- Task 17 minor (deferred): the evidence file records the mobile hero ratio as 0.462 (100svh); the hero is `min-height:92svh` below 860px so the shipped ratio is ~0.502. Both crops pass, so nothing changes.
- Task 17 correction: the "street door at dusk, brass numerals legible" commission note is in `IMPLEMENTATION_PLAN.md:4136` and `task-17-brief.md:65`, NOT `HANDOFF.md` §5.9 as the controller's round-2 brief and the report both stated. The finding stands on the comp caption and the step name regardless.
- Task 17 minor (deferred, carried from Task 16): the `detail` slot is rendered by nothing — no component reads `practice.images.detail`. Both files supplied as the brief requires; the approved comp has no `detail` placeholder either.
- Task 17: complete (commits 8ef8d07..88b5be4, re-review accepted after 2 fix rounds; unit 93/93, build clean, twelve images at the exact `locations.ts` paths all 2400px wide, WebP delivery confirmed, attribution independently verified with zero fabricated credits, `locations.ts`/`open-facts.ts`/read-only roots untouched, port 3000 clear).
- Task 17: the `photography` open fact remains UNRESOLVED, as required. Four typed open facts still stand: clinicians, sharedTeam, selfPayWording, photography.
- Task 18: complete (commits 96f60d8..0790e96, review approved, spec ✅). Genuine RED (`dentists.length === 0`, the file's other two tests passing at RED for the right reasons) then GREEN. Reviewer independently rebuilt, re-ran every suite, and curled the served HTML rather than trusting the tests: seo 6/6, full desktop/mobile E2E 22/22 (was 16), unit 93/93, lint clean, build clean, direction seed `2c7cb46c` intact, two `Dentist` records in PRACTICE_ORDER, no `aggregateRating` or `"review"` in the served output.
- Task 18 minor (deferred): `PracticeJsonLd.tsx` maps `hours.days` through a fixed `{Mo..Fr}` lookup with no fallback. Today's data is exactly `['Mo','Tu','We','Th','Fr']`, but an added code would yield `undefined` and `JSON.stringify` would emit a literal `null` into `dayOfWeek` silently, with no test to catch it. A `satisfies Record<string,string>` or explicit fallback would fail loudly instead.
- Task 18 minor (deferred): `areaServed: 'Berlin'` and `addressCountry: 'DE'` are hardcoded literals from the plan's own template rather than derived from `p.city`. Both values are factually correct and nothing is fabricated; noted only as future hardening.
- Task 18 note (carried forward, matters for Task 20 screenshots): `tests/e2e/global-setup.ts` boots a production server from the existing `.next/` via `next({ dev: false })` and never runs a build, and `playwright.config.ts` has no `webServer` block. A bare `npx.cmd playwright test` after a source change silently serves a STALE build. `npm.cmd run test:e2e` is safe because its script is `npm run build && playwright test`. Always build before any manual Playwright run or screenshot capture.
- Task 18 correction: the open-facts module is `site/lib/openFacts.ts`, not `open-facts.ts` as earlier controller briefs stated.
- Task 19: implementer returned NEEDS_CONTEXT at commit 7683e4a with a genuine Global-Constraint collision, correctly escalated rather than decided unilaterally. E2E 49/50 (desktop 25/25, mobile 24/25), unit 93/93, lint clean, build clean, seed intact, `layout.tsx` untouched.
- Task 19 constraint collision (USER DECISION REQUIRED, resolved): gold eyebrow text on ivory/parchment measured 4.25:1 (ivory) and 3.92:1 (parchment), below the 4.5:1 AA floor for small text — 11 axe `color-contrast` nodes on mobile. This pits the Global Constraint "Eyebrows are Mulish 11px/600/uppercase/.22em tracking **in gold**" against the Global Constraint accessibility floor. Controller independently recomputed 4.25:1 and confirmed the conflict is real. Desktop reported 0 only because axe cannot resolve those grounds through the gradients; same CSS, same defect.
- Task 19 scoping fact that made the decision safe: `--text-gold` is consumed ONLY by TreatmentSchedule, CostPanel, PracticesSection and StepSection — all light grounds. Gold on espresso uses `--gold-light` and is unaffected, so the brass accent survives every dark passage regardless of the choice.
- Task 19 inherited design-system defect: `site/styles/tokens/colors.css:58` comments `--text-gold` as "gold text that stays legible on light" while its value `--gold-deep` `#8F6C2C` measures 4.25:1 at 11px. The token file is read-only authority and must stay byte-identical to root `tokens/`; the fix is a downstream override in `site/app/globals.css`, never an edit to the token.
- Task 19 USER RULING (2026-08-12): option B — `--text-gold: var(--cognac-deep)` `#7E4320` (6.30 parchment / 6.82 ivory). Umber was rejected because `--text-muted` is also umber, which would collapse the eyebrow into secondary body text and destroy the accent. The brass-to-cognac hue shift on light grounds is an accepted, explicit trade. Accessibility floor governs; the eyebrow-in-gold constraint yields on light grounds only.
- Task 19 page change (independent of the ruling): removed `opacity: 0.4` from `LocaleSwitch .pending`, which composited to 2.16:1 — a flat AA failure.
- Task 19 gap found in the PLAN's own test, not the implementation: the brief's `every interactive element is reachable and visibly focused by keyboard` pressed Tab exactly once and inspected a single element — its name promised a sweep it never performed. The implementer added a fifth test walking the whole tab order (desktop 20 scroll-independent interactive elements, each asserted to paint a brass ring). Reviewer verified the claim about the original and accepted the addition as legitimate and valuable.
- Task 19 review (independent, opus): spec ✅ / quality NOT APPROVED at 763c862. Reviewer independently recomputed the contrast ratios (6.822 ivory / 6.299 parchment / 4.252 old — exactly matching the report), confirmed the override sits in `globals.css` not the token file, confirmed all five token files byte-identical to root `tokens/` via `cmp`, confirmed 0 axe violations on both projects, and confirmed `/de` remains an inert `<span>` with `GERMAN_PENDING` untouched.
- Task 19 CRITICAL (in fix loop): `a11y.spec.ts:30-56` reads `getComputedStyle` once, synchronously after Tab, with no reduced-motion emulation and no polling, while `LocationSwitch .option` transitions `box-shadow` over 140ms. Reviewer reproduced 3 failures across 40 mobile runs. The implementer had already diagnosed and fixed this exact race in the new walk test forty lines below but left it in the test it was deviating from. The report's "`--repeat-each=3` → 30 passed" stability claim is therefore not reproducible.
- Task 19 IMPORTANT (in fix loop): the shipped comments and report state "Chromium's mobile emulation removes links from sequential focus navigation" — factually wrong. `devices['iPhone 13'].defaultBrowserType === 'webkit'`, so the mobile project runs WebKit; Chromium at iPhone-13 metrics tabs all 21 stops. The exclusion's conclusion stands (it matches real iOS Safari) but the stated cause misdirects: mobile keyboard coverage is a browser-engine choice in `playwright.config.ts`, not a page defect.
- Task 19 minors (folded into the fix round, non-gating): `hasOutline || boxShadow !== 'none'` is satisfied by a fully transparent zero-size inset shadow, so half the ring assertion is hollow; `boxShadow` counts as a ring unconditionally, and `--shadow-gold` carries the same `181,138,62` triple in a resting hover state (latent false pass); the walk's desktop canary floor is `>= 15` against a measured 20; and the report's `visited=21` including the sticky bar is not reproducible (reviewer measured `visited=20 offered=20 unvisited=[]`, neither sticky control reached — both do draw the ring when checked manually, so a reporting inaccuracy, not a page defect).
- Task 19 minor (deferred, inherited from the plan): `voice.spec.ts` strips `<script>` correctly — the `!` originates in the escaped `<!--` of the seed-`2c7cb46c` direction contract, which must not be edited — but that also removes the two `application/ld+json` blocks from the `!`/emoji scan, and neither voice test ever scanned attributes. Served HTML verified clean today: 0 `!` in ld+json, 0 emoji, 0 in `alt`/`aria-label`/`title`.
- Task 19 minor (deferred): `/de` loses `opacity: 0.4` and now reads as inert from `cursor: not-allowed` and hover-absence alone — weaker on touch. HANDOFF §5.10's actual requirement (never link `/de` to an English page) still holds.
- Task 19: fix round 1/5 (5 addressed, 1 minor open — F1 critical focus-ring race, F2 WebKit/Chromium misdiagnosis, F3 transparent zero-size shadow accepted as a ring, F4 resting `--shadow-gold` false positive, F5 canary floor 15 vs measured 20 all ADDRESSED; F6 report accuracy three-quarters addressed; commits 763c862..66b0e98). Re-reviewer independently re-ran mobile `--repeat-each=20` twice (40/40, versus 3 failures in 40 before), a11y `--repeat-each=10` both projects (100 passed), and mutation-tested the predicate by forcing `BRASS = ['1, 2, 3']` to confirm it fails loudly rather than passing vacuously.
- Task 19 DISPUTE ADJUDICATED — both prior accounts were wrong because the number is not deterministic. The first reviewer said the walk never reaches the sticky-bar controls (measured `visited=20`); the fix implementer said it always does (measured `visited=22`). The re-reviewer replicated the walk in a standalone harness across 13 runs and measured `visited ∈ {22, 20, 21, then 20 × 10}`, with `offered=expected=20` and `unvisited=[]` every time. Mechanism: `Hero.tsx:20-36` publishes `data-past-hero` from a sentinel IntersectionObserver, `StickyBookingBar.tsx:28-36` mirrors it via MutationObserver into React state, and `visibility: hidden` is what keeps the two controls out of the tab order — whether that async chain has delivered by the time Tab passes stop 17 is wall-clock luck. The TEST is sound: `expected` is always the 20 scroll-independent elements and all 20 are always visited.
- Task 19 minor (deferred, DO NOT LOSE): the shipped comment at `site/tests/e2e/a11y.spec.ts:140-143` and report §2 Addition / §6 / §8.6 now assert as measured fact that the walk "does reach both" sticky controls, which held in only 3 of 13 runs. Correct wording is "may reach, timing-dependent", and the honest record is `visited ∈ {20, 21, 22}`. No code change required — comment and report wording only. This claim has now been wrong in both directions; fix it once, accurately.
- Task 19 OPEN LATENT FLAKE IN THE ACCEPTANCE GATE (carry into Task 20 and the final review): roughly 1 full-suite run in 9-15 fails on a timing-dependent assertion in brief-verbatim `conversion.spec.ts` — observed as both `toBeInViewport` (implementer) and `toHaveAttribute` (re-reviewer, reproduced 1 in 15). Candidates are `conversion.spec.ts:45-46` (`#step-room` in viewport plus `aria-current="step"` after a smooth scroll) and `conversion.spec.ts:6` on mobile, which has only 66px of slack at 390x664. Root cause is NOT font swap (fonts are self-hosted via `next/font` with metric-adjusted fallbacks) and NOT `Reveal` (the hero contains none): it is IntersectionObserver/rAF delivery starvation under 8-worker contention, the same sentinel → `data-past-hero` → MutationObserver → React chain proved racy above. The re-reviewer directly observed `data-past-hero` still `false` at `scrollY=4549` when the renderer produced few frames.
- Task 19 NOTE FOR THE TASK 20 HERO GATE: that racy chain is owned by `Hero.tsx`'s sentinel. Consider whether the user-reported smaller-display hero symptom and this timing flake share a cause before treating them as unrelated — but do not assume they do.
- Task 19 minor (deferred): `isPainted` tests the whole `box-shadow` string, so a composite shadow with one fully transparent layer plus a genuine brass ring would be falsely rejected. Not live — every stop's shadow is single-layer today.
- Task 19 minor (deferred): the walk `break`s on any focusable element lacking `data-kbd`, i.e. anything focusable outside the `INTERACTIVE` selector (`<summary>`, `[contenteditable]`, `<audio controls>`, `iframe`). It would silently truncate rather than fail loudly. None on the page today.
- Task 19 minor (deferred): Chromium paints its UA focus ring for one frame before the author `:focus-visible` rule wins, even under reduced motion (measured `solid 3px rgb(194,180,154)` at t=0, brass at t=50ms on the sticky dismiss button). The polling predicate is load-bearing well beyond the LocationSwitch case; a future "simplification" back to a single synchronous read would break more than one element.
- Task 19 minor (deferred, pre-existing, outside Task 19): root `tokens/` has a sixth file `fonts.css` (479 B) with no counterpart under `site/styles/tokens/`. The five that are mirrored are byte-identical. Task 2 ported five of six; fonts are loaded via `next/font` instead. Confirm this was intentional at the final review.
- Task 19 process note: `.superpowers/` is gitignored and only `progress.md` is force-tracked, so `task-19-report.md`'s detailed deviation record lives on disk only and would not survive `git clean -fdx`. This ledger is therefore the durable record and carries every finding above deliberately.
- Task 19: four documented deviations from the plan's verbatim test text, each after probing that the page genuinely met the requirement — RSC payload leaking `<!--` into `body.textContent`; Chromium spelling the 0.01ms reduced-motion floor as `1e-05s` desktop / `0.00001s` mobile; mobile emulation dropping links from tab order; and the location switch drawing `--focus-ring` as an inset box-shadow because its group has `overflow: hidden`. Deviations 3 and 4 are stronger than the plan's originals. Full before/after in `task-19-report.md` §2.
- Task 19: complete (commits 67f7a04..66b0e98, re-review accepted with 1 minor parked; E2E 52/52 desktop 26 + mobile 26 across 15 separate runs, unit 93/93, lint clean, build clean, seed `2c7cb46c` present, all five mirrored token files SHA-256-identical to root `tokens/`, `openFacts.ts` untouched with four facts still open, port 3000 clear).
- Task 19 USER RULING APPLIED AND VERIFIED: `--text-gold: var(--cognac-deep)` `#7E4320` in `site/app/globals.css`, giving 6.822:1 on ivory and 6.299:1 on parchment, both independently recomputed by the reviewer. Axe reports 0 violations on both projects; mobile previously had 11 `color-contrast` nodes. Gold on espresso via `--gold-light` is untouched.
